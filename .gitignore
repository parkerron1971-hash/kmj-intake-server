__pycache__/
*.pyc
*.pyo
*.bak
.env
.env.*
!.env.example
.venv/
venv/
.idea/
.vscode/
.DS_Store
.claude/
# Legacy preview artifacts predating Pass 4.0. Kept locally as
# reference; gitignored so they stop polluting `git status` on every
# branch checkout. Safe to delete from disk if no longer useful.
layout_previews/

# Orphan JS junk — this is a Python project. If Node files appear
# they're from a stray `npm install` and should be cleaned, not tracked.
node_modules/
package.json
package-lock.json
