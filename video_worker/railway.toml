[build]
builder = "DOCKERFILE"
dockerfilePath = "video_worker/Dockerfile"
[deploy]
startCommand = "python video_cloud_renderer.py"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
