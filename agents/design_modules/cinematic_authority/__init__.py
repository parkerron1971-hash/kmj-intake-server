"""Cinematic Authority component library — Pass 4.0f spike.

Codifies the Cathedral aesthetic as composable variants + treatments.
See agents/design_intelligence/cinematic_authority_intelligence.md for
the design vocabulary this library codifies.

Currently scoped to:
  hero/                Hero section variants + render layer

Future scope (full library if spike validates):
  about/, services/, gallery/, testimonials/, cta_band/, footer/
"""
