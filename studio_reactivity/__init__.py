"""Pass 3.8e — Reactivity & Motion Layer.

Injection-time additions to Builder HTML (or any rendered page):
- micro_interactions: hover lifts, link underlines, button states, image zoom
- scroll_behaviors: fade-up reveals, parallax, smooth scroll, sticky CTAs
- strand_gradients: per-strand background gradient character

All composed via inject.inject_reactivity(html, brief).
"""
