"""studio_layouts — 12 deterministic Python layout renderers for Studio's
visual intelligence. Each layout module exposes render(...) with the same
signature; dispatch.py is the single entry point external callers should use.
"""
