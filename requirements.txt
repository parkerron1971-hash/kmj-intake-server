fastapi==0.115.0
uvicorn==0.30.6
anthropic==0.34.2
python-dotenv==1.0.1
apscheduler==3.10.4
httpx==0.27.2
python-multipart==0.0.9
reportlab>=4.0.0
# Fills the OFFICIAL IRS W-9 AcroForm (irs_forms.py). reportlab GENERATES
# PDFs and cannot fill an existing form, and drawing our own W-9-shaped
# page would be producing a facsimile of a government form.
pypdf>=5.0.0
# Pass 4.0b — Director Agent Critique loop: deterministic_checker uses
# BeautifulSoup to parse generated HTML before scoring against rubric.
beautifulsoup4==4.12.3
# Pass 4.0b.5 PART 5 — Slot upload endpoint validates image dimensions
# server-side against SLOT_DEFINITIONS.min_dimensions before accepting
# the upload. Pillow reads JPEG/PNG/WebP/AVIF headers without decoding
# the full image (uses Image.size).
Pillow>=10.4.0
# Auth Phase 1 — decode + verify Supabase user JWTs in auth_supabase.py.
# The [crypto] extra pulls in the `cryptography` package which PyJWT
# requires for asymmetric algorithms (RS256 / ES256). Newer Supabase
# projects sign tokens with RS256 by default, so HS256-only would 401
# every request — see auth_supabase.py JWKS path.
PyJWT[crypto]>=2.8.0
# Phase F.2 v1 — Plaid bookkeeping + reconciliation. plaid-python
# tracks Plaid API versions via SDK release rather than a runtime
# parameter; pin to the latest stable as of 2026-06.
plaid-python>=24.0.0
pywebpush==2.0.0
# Arc 29 — error tracking. Optional at runtime (init is gated on
# SENTRY_DSN in kmj_intake_automation.py); installed so the env var is
# all that's needed to turn capture on. fastapi integration auto-detected.
sentry-sdk[fastapi]>=2.0.0
# Arc 29 — CI test runner (also used locally).
pytest>=8.0.0
# Twilio SMS rail (twilio_sms.py) — outbound via Messaging Service +
# inbound webhook signature validation (RequestValidator).
twilio>=9.0.0
playwright==1.61.0

# Stage 5 anchoring — publishing a ledger fingerprint to Bitcoin via
# OpenTimestamps. Chosen over a paid network because it needs NO account,
# NO credentials and NO fees, and because an auditor verifies the result
# with the standard `ots verify` tool rather than with anything we wrote.
# Pure Python, no native build step on Railway.
opentimestamps>=0.4.5

# Stage 5 anchoring, second adapter — Hedera Consensus Service. The
# NATIVE Python SDK: the older `hedera-sdk-py` wraps the Java SDK via
# pyjnius and needs a JVM, which this image has no reason to carry.
# Optional at runtime — the adapter is only reached when
# LEDGER_ANCHOR_PROVIDER=hedera, and it refuses with a named-missing-var
# error rather than anchoring nowhere.
hiero-sdk-python>=0.1.0
